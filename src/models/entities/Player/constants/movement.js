/**
 * Constantes de movimento usadas pela classe Player
 */
export const PlayerMovementConstants = {
  /**
   * Gravidade Padrão
   * @valor 0.5
   */
  DEFAULT_GRAVITY: 0.5,
  /**
   * Força de pulo padrão
   * @valor -8
   */
  DEFAULT_JUMP_STRENGTH: -8,
  /**
   * Velocidade Padrão
   * @valor 3
   */
  DEFAULT_SPEED: 3,
  /**
   * Direções
   */
  DIRECTION: {
    LEFT: 'LEFT',
    RIGHT: 'RIGHT'
  },

  /**
   * Velocidade de deslize na parede
   */
  WALL_SLIDE_SPEED: 2
};